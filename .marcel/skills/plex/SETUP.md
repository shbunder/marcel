---
name: plex
description: Guide the user through setting up Plex Media Server management
---

The user is asking about Plex, but Plex management is **not yet configured**.

## How to set up Plex management

Marcel can restart and manage a Plex Media Server running as a Docker container. To enable this:

1. **Docker access** — Marcel needs access to the Docker socket on the machine running Plex
2. **Container name** — the Plex container must be named `plex-server`

### What becomes available after setup

- **Restart Plex** — quickly restart the server when it's unresponsive

Tell the user what's needed to get Plex management working.
