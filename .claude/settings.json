{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git branch --list 'issue/*' | sed 's/^/  /' | head -5 || true; if [ -z \"$(git branch --list 'issue/*')\" ]; then echo 'No active issue branches'; fi",
            "timeout": 2
          }
        ]
      }
    ]
  }
}
